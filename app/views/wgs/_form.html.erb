<%= form_for(wg) do |f| %>
  <% if wg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wg.errors.count, "error") %> prohibited this wg from being saved:</h2>

      <ul>
      <% wg.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
